//!O objetivo é apresentar as 52 palavras reservadas,
//!organizadas por classificação de usabilidade considerando as regras da linguagem.
/*
  *OBS:
  *    -Todas as palavras reservadas são escritas em minúsculo;
  *    -Elas não podem ser utilizadas como nome de variáveis.
  !As palavras estarão separadas em classificações de usabilidade!
  
  !Controle de pacotes:
  ?    import --> importa pacotes ou classes para dentro do código;
  ?    package --> especifica a que pacotes todas as classes de um arquivo pertencem;
  !Modificadores de acesso:
  ?    public --> acesso a qualquer classe;
  ?    private -->acesso apenas dentro da classe;
  ?    protected --> acesso por classes no mesmo pacote e subclasses;(uso de herança na linguagem)
  !Primitivos:
  ?    boolean --> verdadeiro ou falso;
  ?    byte --> um inteiro de 8 bits;
  ?    char --> um caractere unicode;
  ?    double --> um número de ponto flutuante 64 bits;
  ?    float --> um número de ponto flutuante de 32 bits;
  ?    int --> um inteiro de 32bits;
  ?    long --> um inteiro de 64 bits;
  ?    short --> um inteiro de 32 bits;
  ?    void --> indica que o método não tem retorno de valor;
  !Modificadores de classes, variáveis ou métodos:
  ?    abstract --> classe que não pode ser instanciada ou método que precisa ser implementado por uma subclasse não abstrata;
               /determino que uma classe vai ter comportamento mas esse comportamento precisa de uma implementação de uma classe filha.
  ?    class --> especifica uma classe;
  ?    extends --> indica a superclasse que a subclasse está estendendo;
           /enquanto uma classe é abstrata a outra precisa extender essa classe.
  ?    final -->  impossibilita que uma classe seja extendida, que um método seja sobrescrito ou que uma variável seja reinicializada;
  ?    implements --> indica as interfaces que uma classe irá implementar;
  ?    interface --> especifica uma interface;
  ?    native --> indica que um método está escrito em uma linguagem dependente de plataforma, como o C por exemplo;
           /DIFICILMENTE UTILIZADA
  ?    new --> instancia(criando) um novo objeto, chamando seu construtor;
  ?    static --> faz um método ou variável pertencer à classe ao invés de às instâncias;
  ?    strictfp -->usada em frente de um método ou classe para indicar que os números de ponto flutuante seguiram as regras de ponto flutuante em todas as expressões;
           /hoje em dia, DIFICILMENTE utilizada
  ?    synchronized -->indica que um método so pode ser acessado por uma thread por vez;
 ?    transient --> impede a serialização de campos;
           /não armazena a informação
  ?    volatile -->indica que uma variável pode ser alterada durante o uso de threads.
  !Controle de fluxo dentro de um bloco de código:
  ?    break --> sai do bloco de codigo em que ele está;
  ?    case --> executa um bloco de código dependendo do teste do switch;
  ?    continue -->
 */
public class Palavras {
    
}
